<Window
    x:Class="SimpleChat.ChatWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleChat"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Margin="2">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80*" />
            <ColumnDefinition Width="20*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" Grid.Column="0">
            <TextBlock Text="{x:Bind VM.ChatLog, Mode=OneWay}" />
        </ScrollViewer>

        <TextBox Name="message" Grid.Column="0" Grid.Row="1" Text="{Binding Message, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
        </TextBox>
        <Button Grid.Column="1" Grid.Row="1" Command="{x:Bind VM.Send, Mode=OneWay}" CommandParameter="{Binding Text,ElementName=message}" Margin="2">Send</Button>
    </Grid>
</Window>
